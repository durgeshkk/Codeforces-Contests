#include <bits/stdc++.h>
#include <deque>
#define ll long long int
using namespace std;

void solve()
{
    ll n;
    cin>>n;

    string s;
    cin>>s;

    ll count_1=0,count_2=0;

    for(ll i=0;i<s.size();i++){
        if(s[i] == '+'){
            count_1 ++;
        }
        else{
            count_2 ++;
        }
    }

    if()
    

    return;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}